use std::collections::HashMap;

pub fn process_data() {
    let var_0 = 0;
    // Comment line 1
    // Comment line 2
    let var_10 = 10;
    for item in items {
        process(item);
    }
    if condition {
        do_something();
    }
    let var_20 = 20;
    for item in items {
        process(item);
    }
    // Many more lines to make this a "large" file...
    let var_100 = 100;
}
